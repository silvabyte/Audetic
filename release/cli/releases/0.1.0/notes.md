# Audetic 0.1.0 (preview)

- Initial binary release layout placeholder for installer + auto-update integration tests.
- Includes baseline CLI, transcription service, and systemd unit templates.
- Checksums and signatures will be updated during the real `make deploy` pipeline.
